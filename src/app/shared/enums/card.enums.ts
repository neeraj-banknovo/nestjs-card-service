export enum CARD_VENDOR {
  PRIVACY = 'PRIVACY',
};

export enum CARD_CATEGORY {
  PHYSICAL = 'PHYSICAL',
  VIRTUAL = 'VIRTUAL',
};

export enum CARD_STATUS {
  ACTIVE = 'ACTIVE',
  CLOSED = 'CLOSED',
};

